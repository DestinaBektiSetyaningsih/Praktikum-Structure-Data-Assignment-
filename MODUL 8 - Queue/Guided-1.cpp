// Note:
/// Queue kebalikan dari stack, queue dia yang masuk pertama, dia juga yang keluar terakhir.
/// Queue FIFO, Stack LIFO
/// front++ dan back++ hanya buat penanda
/// fungsi dequeue -> buat dari depan, jika queue kosong otomatis ke else
/// queueTeller i + 1, jadi indeks yang ke-1 akan berubah jadi NULL, dan NULL akan kehapus. 
/// jika ingin mengetahui brp panjang antrian menggunakan int countQueue
/// Jika tidak ada data atau angka di clear, maka back akan 0
/// Jika viewQueue selama ada isinya brti TRUE